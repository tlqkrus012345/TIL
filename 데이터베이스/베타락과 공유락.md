```
공유락이란 읽기 작업에 사용하는 락으로 여러 트랜잭션이 동시에 읽기만 가능하고 쓰기는 불가능합니다.

SELECT * FROM table_name LOCK IN SHARE MODE;
```

```
베타락이란 읽기/쓰기에 사용하는 락으로 다른 트랜잭션이 읽기, 쓰기가 불가능합니다.

SELECT * FROM table_name FOR UPDATE;
```